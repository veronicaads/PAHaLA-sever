export declare function getHolidays(options: Options): Promise<Holiday[]>;
export declare type Holiday = {
    date: Date;
    name: string;
};
export declare type Options = {
    country: string;
    lang: string;
    start?: Date;
    end?: Date;
    timeout?: number;
};
